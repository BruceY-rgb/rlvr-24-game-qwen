.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store

# Runtime checkpoints can become large; keep scripts and small eval artifacts in git.
outputs/qwen24-grpo/
outputs/sanity/
outputs/eval/

# Hugging Face / model caches
.cache/
models/
checkpoints/

# Large model weights & training state — too big for git (keep on OBS/ModelArts)
checkpoint-*/
*.safetensors
*.pt
*.bin

# Large raw per-sample eval dumps — keep *_metrics.json + summary.*, drop the big ones
outputs/**/countdown_*.jsonl
outputs/**/countdown_*.csv
outputs/modelarts_ablation/
